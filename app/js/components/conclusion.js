import React from 'react';

export default class Conclusion extends React.Component {
  render() {
    return (
        <div>
         <h1>Conclusion</h1>
         <p>So neural networks are a type of machine learning device which allows programs to find solutions to problems by incrementally judging themselves and improving.</p>
      </div>
    );
  }
}